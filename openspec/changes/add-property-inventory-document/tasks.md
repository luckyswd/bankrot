## 1. Шаблон

- [x] 1.1 Сконвертировать `~/Downloads/new_docs/3. Опись имущества гражданина.doc` в `.docx` через `soffice --headless --convert-to docx` во временный каталог. Проверка: файл открывается как zip, `word/document.xml` разбирается как XML, в тексте видны все разделы формы от шапки до «Исключительные права».
- [x] 1.2 Скриптом по `word/document.xml` заменить 12 окрашенных прогонов на плейсхолдеры и снять с них `<w:color>`: шапка и подпись — `{{ fullNameGenitive }}`, дата описи (красный прогон «02» июля 2025 вместе с идущим следом « г.») — `{{ propertyInventoryDateText }}`, таблица «Информация о гражданине» — `{{ fullName }}`, `{{ previousFullName }}`, `{{ birthDate }}`, `{{ birthPlace }}`, `{{ inn }}`, `{{ snils }}`, `{{ fullRegistrationAddress }}`, `{{ passportSeries }}`, `{{ passportNumber }}`. Проверка: в документе ровно 11 разных плейсхолдеров, каждый лежит в одном прогоне, окрашенных прогонов не осталось.
- [x] 1.3 Положить готовый файл как `backend/src/document-templates/judicial_report/3. Опись имущества гражданина.docx` и выполнить в контейнере `bankruptcy-php` `make templates-sync`. Проверка: команда печатает «Записей создано: 1», повторный запуск — нули, в базе появилась запись с названием «3. Опись имущества гражданина» и категорией `judicial_report`.

## 2. Тест шаблона

- [x] 2.1 Добавить `backend/tests/Service/Templates/PropertyInventoryTemplateTest.php` по образцу `CreditorsRegisterTemplateTest`: собрать дело должника с ФИО, родительным падежом, датой и местом рождения, ИНН, СНИЛС, адресом регистрации, паспортом и датой описи, сгенерировать документ из шаблона репозитория. Проверка: тест утверждает, что в тексте нет `{{` и `${`, XML валиден, подставлены ожидаемые значения таблицы «Информация о гражданине», шапка и подпись содержат родительный падеж, строка описи — «проведена «02» июля 2025 г.», а окрашенных прогонов в документе нет.
- [x] 2.2 Проверить сценарии незаполненных данных: дело без смены фамилии даёт «—» в строке «Ранее присвоенные фамилия, имя, отчество», дело без даты описи не оставляет маркеров. Проверка: оба утверждения — отдельными тестами, `make test` зелёный.

## 3. Проверки и приёмка

- [x] 3.1 Прогнать в контейнере `bankruptcy-php` `make stan`, `make lint`, `make test` и исправить все ошибки. Проверка: все три команды завершаются без ошибок.
- [x] 3.2 Пройти сквозную проверку по `openspec/specs/documents/end-to-end-check`: через интерфейс заполнить моковыми данными поля, которые подставляются в опись (данные должника на вкладке «Основная информация» и дату описи на вкладке «Судебка → Процедура»), сохранить дело без ошибок, открыть вкладку «Судебка → Отчёт», убедиться, что документ «3. Опись имущества гражданина» есть в списке со счётчиком заполненности, скачать его и сверить с карточкой дела и с исходником заказчика. Проверка: в файле нет `{{` и `${`, значения совпадают с введёнными, вёрстка формы совпадает с исходником, ошибок в консоли и сети нет. Моковые данные не откатывать.
