## 1. Backend: полис в справочнике финансовых управляющих

- [x] 1.1 Добавить enum `src/Entity/Enum/InsuranceStatus.php` (`valid`, `expired`, `missing`) и в `src/Entity/FinancialManager.php` свойства `insuranceContractNumber` (string 255), `insuranceContractDate`, `insuranceStartDate`, `insuranceEndDate` (date) — все nullable, с геттерами и сеттерами, плюс методы `getInsuranceStatus(DateTimeInterface $today)` и `getInsuranceContractDescription()`. Проверка: юнит-тест `tests/Entity/FinancialManagerTest.php` покрывает три статуса (срок вчера, сегодня, пустой) и оба сценария описания полиса из `specs/reference-books/financial-managers`.
- [x] 1.2 Сгенерировать миграцию для `financial_managers` (`make db-diff`, четыре nullable-колонки). Проверка: `make db-migrate` применяет миграцию, `doctrine:migrations:execute --down` откатывает её без ошибок.
- [x] 1.3 В `src/Controller/FinancialManagerController.php` принимать четыре поля полиса в `setFinancialManagerFields()` (даты `Y-m-d`, пустая строка — `null`), отдавать их в `serializeFinancialManagerFull()`, а `insuranceEndDate` и `insuranceStatus` — ещё и в `serializeFinancialManager()`; текущую дату брать как `new DateTimeImmutable('today')`. Проверка: функциональный тест `tests/Controller/FinancialManagerControllerTest.php` — создание с полисом, очистка номера, `insuranceStatus` в списке для сроков «вчера», «сегодня», «не задан».

## 2. Backend: поля дела

- [x] 2.1 Добавить в `src/Entity/Contracts.php` семь свойств группы `judicial_procedure_initiation`: `procedureInitiationEfrsbMessageNumber`, `procedureInitiationEfrsbMessageDate`, `procedureInitiationKommersantIssueNumber`, `procedureInitiationKommersantAdNumber`, `procedureInitiationKommersantPublicationDate`, `procedureInitiationKommersantPublicationCost` (decimal 15,2), `procedureInitiationCreditorsNotificationDate` — с `#[Groups]`, геттерами и сеттерами; даты внести в `$dateFields` в `src/Service/ContractorService.php`. Проверка: тест в `tests/Controller/ContractsJudicialStagesControllerTest.php` сохраняет поля через `PUT` и получает их в разделе `judicial_procedure_initiation` через `GET`.
- [x] 2.2 Добавить enum `src/Entity/Enum/ProcedureExtensionStatus.php` и в `Contracts.php` 16 свойств группы `judicial_procedure`: `procedureExtensionStatus`, `procedureExtensionDates` (json), `propertyInventoryDate`, `zagsDepartment` (string 500), `zagsCertificatePeriodFrom`, `zagsCertificatePeriodTo`, `claimsIncludedCount`, `claimsRejectedCount` (int), `efrsbExpensesAmount`, `efrsbExpensesPaid`, `postalExpensesAmount`, `postalExpensesPaid`, `newspaperExpensesPaid`, `futureEfrsbExpensesAmount`, `futureEfrsbExpensesPaid` (decimal 15,2), `bankruptcySignsEfrsbPublicationDate`; даты — в `$dateFields`; для пары статус/даты продления — отдельная ветка `updateContractFields()` с вызовом `Contracts::changeProcedureExtension()` и константа `Contracts::DEFAULT_ZAGS_DEPARTMENT`. Проверка: тесты сценариев «Две даты продления» и «Смена варианта удаляет даты» из `specs/contracts/judicial-stages` в `tests/Controller/ContractsJudicialStagesControllerTest.php`.
- [x] 2.3 Сгенерировать миграцию для `contracts` (23 nullable-колонки). Проверка: `make db-migrate` применяет миграцию на локальной базе, существующее дело открывается через `GET /api/v1/contracts/{id}`, откат проходит без ошибок.
- [x] 2.4 Добавить в `Contracts.php` вычисляемые геттеры с `#[Groups]` на методе и без сеттеров: `getRegistryClosingDate()` (группа `judicial_procedure_initiation`; перенос на два месяца с ограничением дня концом месяца), `getClaimsConsideredCount()`, `getNewspaperExpensesAmount()`, `getEfrsbExpensesUnpaid()`, `getPostalExpensesUnpaid()`, `getNewspaperExpensesUnpaid()`, `getFutureEfrsbExpensesUnpaid()` (группа `judicial_procedure`; остаток в копейках целыми). Проверка: юнит-тест на даты 08.04.2026 → 08.06.2026, 31.12.2025 → 28.02.2026, 07.02.2025 → 07.04.2025 и остаток 560.55 − 29.67 = 530.88; функциональный тест «Попытка записать дату закрытия напрямую» в `tests/Controller/ContractsJudicialStagesControllerTest.php`; юнит-тест — `tests/Entity/ContractsComputedValuesTest.php`.

## 3. Backend: шаблонизатор и формулировки отчёта

- [x] 3.1 В `src/Service/Templates/CustomFunction.php` добавить маску `ММММР` (заменяется раньше `ММММ`, название из `DateHelperService::getMonthNameGenitive()`). Проверка: юнит-тест `tests/Service/Templates/CustomFunctionTest.php` на четыре сценария `specs/documents/template-functions`, ожидаемое значение строится от текущей даты.
- [x] 3.2 В `src/Service/Templates/EntityDataResolver.php` для однокомпонентного пути заменить проверку `empty($value)` на `$value === ''`. Проверка: тест — поле со значением 0 разрешается в «0», пустое поле — в пустую строку; существующие тесты генерации проходят.
- [x] 3.3 Добавить `DateHelperService::formatGenitive(?DateTimeInterface): string` и `src/Service/MoneyHelperService.php` со статическим `format(?string): string`. Проверка: юнит-тесты — 2025-07-02 → «02» июля 2025 г., `null` → пустая строка; `"7866.05"` → «7 866,05» с неразрывным пробелом, `"-10.00"` → «-10,00», `null` → «0,00».
- [x] 3.4 Создать `src/Service/Templates/JudicialReport/ReportMethods.php` с методами `previousFullName`, `procedureExtensionText`, `spouseInstrumental`, `shortFullNameGenitive`, `procedureInitiationDateShort`, `zagsDepartmentText`, `zagsCertificatePeriodText`. Проверка: юнит-тест `tests/Service/Templates/ReportMethodsTest.php` покрывает таблицы вариантов из `specs/documents/financial-manager-report`: пол × семейное положение без двойного пробела, три варианта продления, неполный период ЗАГС, фамилия не менялась.
- [x] 3.5 Добавить в `ReportMethods` методы `notifiedCreditorsList` (кредиторы досудебной стадии без повторов, `Creditor::getNameGenitive()`), `registryCreditorsCount` (разные кредиторы в требованиях) и `minorChildrenDependantsText` (0, 1, 2, 3–10 собирательным числительным, больше 10 цифрами). Проверка: юнит-тесты на 0, 1, 2, 3 и 11 детей, на повтор кредитора и на пустой список.
- [x] 3.6 Добавить в `Contracts.php` геттеры-обёртки для методов из 3.4–3.5, шесть геттеров `*DateText` через `DateHelperService::formatGenitive()` и двенадцать `*AmountText` / `*PaidText` / `*UnpaidText` через `MoneyHelperService::format()`; в `FinancialManager` — геттер для `{{ financialManager.insuranceContractDescription }}` (сделан в 1.1). Проверка: тест `tests/Service/Templates/FinancialManagerReportPlaceholdersTest.php` разрешает через `EntityDataResolver` каждый плейсхолдер из таблиц `specs/documents/financial-manager-report` на собранном в памяти деле и сравнивает с примерами.
- [x] 3.7 В `src/Service/Templates/DocumentTemplateProcessor.php` вынести перечень классов формулировок из `analyzeMethodRecursive()` в константу и добавить в неё `ReportMethods`. Проверка: тест `tests/Service/Templates/DocumentTemplateProcessorFieldsTest.php` — `extractFields()` для шаблона с `{{ registryClosingDateText }}` и `{{ spouseInstrumental }}` возвращает `judicial_procedure_initiation.procedureInitiationKommersantPublicationDate`, `basic_info.maritalStatus` и `basic_info.gender`.

## 4. Frontend: справочник финансовых управляющих

- [x] 4.1 Создать `src/components/shared/InsuranceStatusAlert.tsx` (показывается для `expired` и `missing`, тексты из спецификации) и добавить в `src/components/Modals/FinancialManagerFormModal.tsx` группу «Договор страхования ответственности»: номер и три даты через `DatePickerInput`, предупреждение над группой, `invalidateQueries({ queryKey: ["references"] })` после сохранения. Проверка: `tsc --noEmit` и `npm run build` без ошибок; ручная проверка (полис сохраняется и открывается повторно, при сроке «вчера» видно «Полис страхования истёк …») — в 7.1.
- [x] 4.2 В `src/components/databases/FinancialManagersDatabase.tsx` показать отметку в строке управляющего со статусом `expired` или `missing`. Проверка: `tsc --noEmit` и `npm run build`; ручная проверка (отметка появляется и исчезает после внесения действующего полиса) — в 7.1.

## 5. Frontend: карточка дела

- [x] 5.1 Расширить `IntroductionFields` и `ProcedureFields` в `src/components/ClientCard/types.ts`; в `helpers.ts` дополнить `defaultIntroduction`, `defaultProcedure` (с текстом ЗАГС по умолчанию) и `convertApiDataToFormValues()`: раздел `judicial_procedure_initiation` поле за полем, даты `judicial_procedure` — через `formatDate`. Проверка: `tsc --noEmit` и `npm run build`; ручная проверка (значения всех новых полей переживают сохранение и перезагрузку страницы) — в 7.1. Формулы живого пересчёта — в `ClientCard/utils/judicialCalculations.ts`.
- [x] 5.2 В `src/components/ClientCard/JudicialTab/Introduction.tsx` подключить новый `IntroductionReportSections.tsx` — аккордеон, раскрытый по умолчанию, с блоками «Публикации» (ЕФРСБ, Коммерсантъ), «Уведомление кредиторов почтой» и «Дата закрытия реестра» (живой расчёт через `date-fns/addMonths`, заглушка «Заполните дату публикации в «Коммерсантъ»»); подписи с `htmlFor="judicial_procedure_initiation.<поле>"`. Проверка: `tsc --noEmit`; ручная проверка сценариев «Пример заказчика», «В конечном месяце нет такого числа», «Пересчёт до сохранения», «Отрицательная стоимость» — в 7.1.
- [x] 5.3 В `src/components/ClientCard/JudicialTab/Procedure.tsx` подключить новый `ProcedureReportSections.tsx` — аккордеон, раскрытый по умолчанию, с блоками «Продление процедуры» (выбор варианта и список дат через `Controller` без подписи `htmlFor` у списка), «Опись имущества» и «Ответы из госорганов → ЗАГС» (текст по умолчанию, проверка периода). Проверка: `tsc --noEmit`; ручная проверка сценариев «Две даты продления», «Смена варианта удаляет даты», «Текст по умолчанию», «Период перепутан местами» — в 7.1.
- [x] 5.4 В `ProcedureReportSections.tsx` добавить блоки «Результаты рассмотрения требований» (живой итог), «Расходы на проведение процедуры» (четыре позиции, размер расходов на газеты из стоимости публикации в «Коммерсантъ», живой остаток, предупреждение «Погашено больше размера обязательства») и «Фиктивные и преднамеренные признаки банкротства». Проверка: `tsc --noEmit`; ручная проверка сценариев «Подсчёт итога», «Остаток считается сам», «Расходы на газеты из публикации», «Погашено больше, чем должно» — в 7.1.
- [x] 5.5 В `src/components/ClientCard/JudicialTab/Report.tsx` читать документы из `contractData.judicial_report`, убрать «Раздел в разработке», показать `InsuranceStatusAlert` выбранного управляющего над списком документов; добавить тот же компонент под выбором управляющего в `General/ManagerInfo.tsx`. Проверка: вручную сценарии «Загруженный шаблон отчёта», «Юрист готовит отчёт с просроченным полисом», «Управляющий не выбран».

## 6. Шаблон отчёта

- [x] 6.1 Взять `new_docs/1_Отчет_финансового_управляющего_о_результатах_проведения_реализации.docx` (сконвертированный из `.doc`) и расставить плейсхолдеры шапки, сведений о должнике, управляющем, разделов 4.3 и 4.5 по `specs/documents/financial-manager-report`: каждый плейсхолдер вводится одним куском, подставляемые фрагменты — обычным цветом, ручные остаются красными. Проверка: скрипт по `word/document.xml` находит каждый плейсхолдер этих разделов целиком в одном run.
- [x] 6.2 Расставить плейсхолдеры разделов 4.7.1, 4.8, 4.9 и 5 и сохранить шаблон как `backend/src/document-templates/1_financial_manager_report.docx`. Проверка: тот же скрипт по всему документу (56 плейсхолдеров, 50 уникальных переменных видит PhpWord); файл открывается в LibreOffice — проверка в Word остаётся за приёмкой в 7.1.
- [x] 6.3 Добавить тест `tests/Service/Templates/FinancialManagerReportTemplateTest.php`: `DocumentTemplateProcessor::processTemplate()` по шаблону из 6.2 и собранному в памяти делу со всеми полями. Проверка: в `word/document.xml` результата нет `{{`, есть «07» апреля 2025 г., «530,88», «двое несовершеннолетних детей», а четыре ручных фрагмента остаются красными; `make test` зелёный. Тестовое дело собирает `tests/Service/Templates/ReportContractFactory.php`.

## 7. Приёмка и проверки

- [x] 7.1 Загрузить шаблон через `/documents` в категорию «Судебка отчет» локально, заполнить тестовое дело и скачать отчёт с вкладки «Судебка → Отчёт». Проверка: у шаблона виден список незаполненных полей, клик по «Дата публикации» ведёт на «Введение процедуры» к блоку «Публикации»; готовый документ совпадает с примерами спецификации.
- [x] 7.2 Прогнать в контейнере `bankruptcy-php` `make stan`, `make lint`, `make test`, во фронтенде — `npm run build`, и исправить все ошибки. Проверка: все четыре команды завершаются без ошибок.
- [x] 7.3 Пройти сквозную проверку по `openspec/specs/documents/end-to-end-check`: заполнить через интерфейс дело 209 и справочник финансовых управляющих моковыми данными, сохранить, скачать отчёт с вкладки «Судебка → Отчёт», открыть и сверить с введёнными данными. Проверка: индикатор «Заполнено: 56 из 56»; все 56 подстановок совпадают с введёнными значениями, `{{` не осталось, красным выделены только ручные фрагменты, вёрстка 8 страниц не нарушена. Найденная ошибка — форма управляющего не отправляла `fioGenitive` — исправлена в `src/components/Modals/FinancialManagerFormModal.tsx`.
