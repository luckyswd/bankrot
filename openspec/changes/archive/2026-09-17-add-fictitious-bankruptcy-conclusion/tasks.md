## 1. Шаблон

- [x] 1.1 Сконвертировать `~/Downloads/new_docs/5_Заключение_о_наличии_отсутствии_признаков_фиктивного_или_преднамеренного.doc` в `.docx` и скриптом по `word/document.xml` заменить окрашенные прогоны на плейсхолдеры, сняв цвет: дата составления и конец исследуемого периода — `{{ ТЕКУЩАЯ_ДАТА('«дд» ММММР гггг') }}`, суд в таблице — `{{ court.name }}` вместо статического «Арбитражный суд » с выделенным хвостом, суд в предложении о возбуждении дела — `{{ court.shortName }}`, номер дела — `{{ caseNumber }}`, даты судебных актов — `{{ procedureInitiationDateForPublication }}`, страхование — `{{ financialManager.insuranceContractDescription }}`, сведения о должнике — `{{ fullName }}`, `{{ birthDate }}`, `{{ birthPlace }}`, `{{ inn }}`, `{{ snils }}`, `{{ fullRegistrationAddress }}`, дата возбуждения — `{{ caseInitiationDate }}`, все вхождения ФИО должника в родительном падеже, включая неразмеченные, — `{{ fullNameGenitive }}`. Проверка: в шаблоне не осталось окрашенных прогонов и цветных тегов, ФИО из образца в тексте нет, XML валиден.
- [x] 1.2 Положить файл как `backend/src/document-templates/judicial_report/5. Заключение о наличии (отсутствии) признаков фиктивного или преднамеренного банкротства.docx` и выполнить `make templates-sync`. Проверка: «Записей создано: 1», повторный запуск — нули, запись с нужным названием и категорией `judicial_report`.

## 2. Тест шаблона

- [x] 2.1 Добавить `backend/tests/Service/Templates/FictitiousBankruptcyConclusionTemplateTest.php`: дело с судом, номером дела, данными должника, датой возбуждения дела, датой определения о введении процедуры и управляющим с договором страхования. Проверка: тест утверждает, что маркеров не осталось, XML валиден, окрашенных прогонов нет, подставлены ФИО и родительный падеж во всех предложениях, наименование суда в таблице и короткая форма в предложении о возбуждении дела, номер дела, даты судебных актов, дата возбуждения в формате ДД.ММ.ГГГГ и договор страхования.
- [x] 2.2 Проверить документ на пустом деле: незаполненные данные не оставляют маркеров, текст формы сохраняется. Проверка: отдельный тест, `make test` зелёный.

## 3. Проверки и приёмка

- [x] 3.1 Прогнать в контейнере `bankruptcy-php` `make stan`, `make lint`, `make test`; исправить все ошибки. Проверка: все команды завершаются без ошибок.
- [x] 3.2 Пройти сквозную проверку по `openspec/specs/documents/end-to-end-check`: открыть карточку дела с заполненными данными, убедиться, что документ есть на вкладке «Судебка → Отчёт» со счётчиком заполненности, скачать его и сверить с карточкой дела и исходником заказчика. Проверка: в файле нет `{{` и `${`, значения совпадают с карточкой, вёрстка совпадает с исходником, ошибок в консоли и сети нет. Моковые данные не откатывать.
