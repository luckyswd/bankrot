## 1. Backend: раскладка файлов и регистрация записей

- [x] 1.1 Перенести файлы из `backend/src/document-templates` в папки стадий через `git mv`, дав им имена из названий существующих записей (`judicial_procedure_initiation/1. Публикация ЕФРСБ.docx`, `judicial_procedure_initiation/9. Заявка на открытие спецсчета в Совкомбанк.xlsx`, `judicial_report/1. Отчёт финансового управляющего о результатах реализации имущества.docx`, `judicial_report/2. Реестр требований кредиторов.docx` и так далее). Проверка: в каталоге только папки стадий, имя каждого файла без расширения совпадает с названием записи в базе, кроме шаблона отчёта, записи для которого ещё нет.
- [x] 1.2 В `src/Service/Templates/DocumentTemplateSynchronizer.php` перевести копирование из каталога образа на обход подпапок с сохранением относительного пути и созданием папок. Проверка: `tests/Service/Templates/DocumentTemplateSynchronizerTest.php` — файл из `judicial_report/` копируется в такую же подпапку, скрытые файлы пропускаются, перенос из `var/document-templates` работает как прежде.
- [x] 1.3 Создать `src/Service/Templates/DocumentTemplateRegistrar.php`: обход папок стадий, отбор `.docx` и `.xlsx`, поиск записи через `DocumentTemplateRepository::findByNameAndCategory()`, обновление пути, создание недостающих, удаление записей с несуществующим файлом; возвращает `array{created: int, updated: int, removed: int}`. Проверка: функциональный тест `tests/Service/Templates/DocumentTemplateRegistrarTest.php` на сценарии спецификации — новый файл создаёт запись, файл с названием существующей записи переставляет путь без дубля, запись без файла удаляется, повторный запуск даёт нули.
- [x] 1.4 Вызвать регистратор в `src/Command/SyncDocumentTemplatesCommand.php` после раскладки файлов и вывести сводку по созданным, обновлённым и удалённым записям. Проверка: `app:document-templates:sync` локально печатает обе сводки, после первого запуска список шаблонов на `/documents` содержит все файлы репозитория, после второго — счётчики нулевые.
- [x] 1.5 Удалить из `src/Controller/DocumentTemplateController.php` действия `create` и `update` вместе с атрибутами OpenAPI, приватными `getUploadDir()` и `ensureUploadDirExists()` и ставшими лишними импортами. Проверка: в `tests/Controller/DocumentTemplateControllerTest.php` тесты загрузки и обновления заменены на проверки 405 для `POST /api/v1/document-templates` и `PUT /api/v1/document-templates/{id}`; список, скачивание и генерация по-прежнему проходят.
- [x] 1.6 Перевести на новые пути тесты, которые открывают шаблоны файлом: `FinancialManagerReportTemplateTest`, `CreditorsRegisterTemplateTest`, `TemplateBlocksProcessingTest`. Проверка: `make test` зелёный.

## 2. Деплой

- [x] 2.1 В `.github/workflows/staging.yml` перенести `make templates-sync` после `make db-migrate` и объяснить в комментарии, что шаг пишет в базу. Проверка: YAML разбирается, порядок шагов — сброс кэша, миграции, шаблоны, `make cc`.

## 3. Frontend: страница шаблонов

- [x] 3.1 Убрать из `src/components/DocumentsPage.tsx` карточку «Загрузить новый шаблон» и связанный код: состояние выбранного файла, названия, категории и признака загрузки, обработчики выбора и отправки, ставшие лишними импорты. Проверка: `npx tsc --noEmit` и `npm run build` без ошибок; на странице остаются поиск, фильтр по категории, просмотр и скачивание.

## 4. Проверки и приёмка

- [x] 4.1 Прогнать в контейнере `bankruptcy-php` `make stan`, `make lint`, `make test`, во фронтенде — `npx tsc --noEmit` и `npm run build`; исправить все ошибки. Проверка: все команды завершаются без ошибок.
- [x] 4.2 Пройти сквозную проверку по `openspec/specs/documents/end-to-end-check`:
  - выполнить `make templates-sync` на локальной базе и сверить список шаблонов: названия и категории прежние, дублей нет, шаблон отчёта финансового управляющего появился сам;
  - открыть `/documents` и убедиться, что блока загрузки нет, а поиск, фильтр, просмотр и скачивание работают;
  - скачать реестр требований кредиторов с вкладки «Судебка → Отчёт» дела 209, открыть и сверить с данными дела.

  Проверка: в файле нет `{{` и `${`, итоги совпадают с введёнными суммами, ошибок в консоли и сети нет. Моковые данные не откатывать.
