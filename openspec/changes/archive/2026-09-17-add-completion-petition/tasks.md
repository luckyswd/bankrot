## 1. Backend: формулировки заседания

- [x] 1.1 В `src/Service/Templates/JudicialReport/ReportMethods.php` добавить два текста заседания по рассмотрению отчёта: «дд» месяц гггг г. в ЧЧ час. ММ мин. и дд месяц гггг года в ЧЧ час. ММ мин.; пустая строка, если заседание не назначено. В `Contracts` — геттеры-обёртки. Проверка: юнит-тесты в `ReportMethodsTest` на заполненную дату и на её отсутствие.

## 2. Шаблон

- [x] 2.1 Сконвертировать `~/Downloads/new_docs/Ходатайство_о_завершении_реализации_имущества.doc` в `.docx` и подставить плейсхолдеры: ФИО должника в родительном падеже, `{{ court.name }}` и `{{ court.address }}` в шапке, `{{ caseNumber }}`, дата заседания в двух формах, `{{ procedureInitiationJudge }}`, вводный абзац — `{{ startNotificationText }}`, суд в просительной части — `{{ court.shortName }}`. Зелёный цвет снять, красный на ручных частях сохранить. Проверка: в шаблоне нет ФИО, номера дела и адреса из образца, зелёных прогонов не осталось, красные остались, XML валиден.
- [x] 2.2 Положить файл как `backend/src/document-templates/judicial_report/7. Ходатайство о завершении процедуры реализации имущества гражданина.docx` и выполнить `make templates-sync`. Проверка: «Записей создано: 1», повторный запуск — нули, запись с нужным названием и категорией `judicial_report`.

## 3. Тест шаблона

- [x] 3.1 Добавить `backend/tests/Service/Templates/CompletionPetitionTemplateTest.php`: дело с судом и адресом, номером дела, судьёй, датой и временем заседания, данными должника и датами судебных актов. Проверка: тест утверждает, что маркеров не осталось, XML валиден, зелёных прогонов нет, подставлены шапка, обе формы даты заседания, судья, вводный абзац и суд в просительной части.
- [x] 3.2 Проверить дело без заседания: место даты пустое, маркеров нет. Проверка: отдельный тест, `make test` зелёный.

## 4. Проверки и приёмка

- [x] 4.1 Прогнать в контейнере `bankruptcy-php` `make stan`, `make lint`, `make test`; исправить все ошибки. Проверка: все команды завершаются без ошибок.
- [x] 4.2 Пройти сквозную проверку по `openspec/specs/documents/end-to-end-check`: заполнить через интерфейс дату и время заседания и судью, если они пусты, сохранить дело, открыть вкладку «Судебка → Отчёт», убедиться, что документ есть в списке со счётчиком заполненности, скачать его и сверить с карточкой дела и исходником заказчика. Проверка: в файле нет `{{` и `${`, значения совпадают с карточкой, вёрстка совпадает с исходником, ошибок в консоли и сети нет. Моковые данные не откатывать.
