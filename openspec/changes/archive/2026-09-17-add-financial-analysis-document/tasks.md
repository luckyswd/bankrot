## 1. Backend: поля и тексты

- [x] 1.1 В `src/Entity/Contracts.php` добавить поля `financialAnalysisSupplementDate` (DATE, `#[Groups([BankruptcyStage::JUDICIAL_PROCEDURE->value])]`) и `isSocialPaymentsRecipient` (BOOLEAN, `#[Groups([BankruptcyStage::BASIC_INFO->value])]`) с геттерами и сеттерами. Проверка: `make stan` зелёный.
- [x] 1.2 Сгенерировать миграцию Doctrine на две колонки и применить её. Проверка: миграция добавляет ровно две колонки и не трогает существующие, `make db-migrate` проходит.
- [x] 1.3 Добавить `financialAnalysisSupplementDate` в список `dateFields` в `ContractorService::updateContractFields()`. Проверка: `PUT /api/v1/contracts/{id}` сохраняет дату, значение возвращается в группе `judicial_procedure`.
- [x] 1.4 В `src/Service/Templates/JudicialReport/ReportMethods.php` добавить тексты: строка о дополнении анализа («Дополнен «дд» месяц гггг г.» либо пустая), фраза о получателе социальных выплат («является» / «не является»), регион прожиточного минимума в родительном падеже по адресу регистрации и год прожиточного минимума из даты финансового анализа; в `Contracts` — геттеры-обёртки. Проверка: юнит-тесты в `ReportMethodsTest` на все случаи, включая пустые и «другой регион».

## 2. Frontend: поля на вкладках

- [x] 2.1 Добавить флаг «Получатель пенсий, пособий и иных социальных выплат» на вкладку «Основная информация» по образцу соседних флагов. Проверка: `npx tsc --noEmit`, флаг сохраняется и перечитывается.
- [x] 2.2 Добавить дату «Дата дополнения финансового анализа» в блок отчётных секций вкладки «Судебка → Процедура» рядом с датой публикации в ЕФРСБ о признаках банкротства. Проверка: `npx tsc --noEmit`, дата сохраняется и перечитывается.

## 3. Шаблон

- [x] 3.1 Написать скрипт подготовки шаблона из `~/Downloads/new_docs/6_Анализ_финансового_состояния_гражданина.docx`: склейка групп прогонов, подстановка плейсхолдеров шапки, титула, таблицы официальной информации, сведений о должнике, судебных актов, дополнения анализа, социальных выплат, региона и года прожиточного минимума; снятие зелёного цвета везде и красного только на заменённых местах. Проверка: в шаблоне нет ФИО, номера дела, адреса и реквизитов страхования из образца; зелёных прогонов не осталось; красные остались только на ручных частях; XML валиден.
- [x] 3.2 Положить файл как `backend/src/document-templates/judicial_report/6. Анализ финансового состояния гражданина.docx` и выполнить `make templates-sync`. Проверка: «Записей создано: 1», повторный запуск — нули, запись с нужным названием и категорией `judicial_report`.

## 4. Тест шаблона

- [x] 4.1 Добавить `backend/tests/Service/Templates/FinancialAnalysisTemplateTest.php`: дело с судом и его адресом, номером дела, данными должника, датой публикации о признаках банкротства, датой дополнения, флагом социальных выплат, адресом регистрации в Санкт-Петербурге и управляющим с договором страхования. Проверка: тест утверждает, что маркеров не осталось, XML валиден, зелёных прогонов нет, подставлены шапка, сведения о должнике, судебные акты, дополнение, фраза о выплатах, регион и год прожиточного минимума.
- [x] 4.2 Проверить дело без дополнения анализа и с регистрацией в Ленинградской области: строки «Дополнен» нет, регион — «Ленинградской области». Проверка: отдельные тесты, `make test` зелёный.

## 5. Проверки и приёмка

- [x] 5.1 Прогнать в контейнере `bankruptcy-php` `make stan`, `make lint`, `make test`, во фронтенде — `npx tsc --noEmit` и `npm run build`; исправить все ошибки. Проверка: все команды завершаются без ошибок.
- [x] 5.2 Пройти сквозную проверку по `openspec/specs/documents/end-to-end-check`: заполнить через интерфейс моковыми данными новые поля, сохранить дело без ошибок, открыть вкладку «Судебка → Отчёт», убедиться, что документ есть в списке со счётчиком заполненности, скачать его и сверить с карточкой дела и исходником заказчика. Проверка: в файле нет `{{` и `${`, значения совпадают с введёнными, вёрстка совпадает с исходником, ошибок в консоли и сети нет. Моковые данные не откатывать.
