## 1. Backend: сущность и хранение

- [x] 1.1 Создать перечисления `src/Entity/Enum/PropertyKind.php` (недвижимое, движимое) и `src/Entity/Enum/PropertySubtype.php` (пять подвидов недвижимости и семь подвидов транспорта) с подписями для документов и методом, отдающим подвиды нужного вида. Проверка: юнит-тест на соответствие подвида виду.
- [x] 1.2 Создать сущность `src/Entity/ContractsProperty.php` (таблица `contracts_property`, наследник `BaseEntity`) с полями вида, подвида, наименования, вида собственности, местонахождения, площади, идентификационного номера, залога, двух стоимостей, признака исключения, основания и стоимости исключаемого; связь `ManyToOne` к `Contracts` и `OneToMany` с `cascade: ['persist', 'remove']` и `orphanRemoval` в `Contracts`; атрибуты `#[Groups([BankruptcyStage::JUDICIAL_PROCEDURE->value])]`. Проверка: `make stan` зелёный.
- [x] 1.3 Сгенерировать миграцию Doctrine на таблицу `contracts_property` и применить её. Проверка: миграция создаёт одну таблицу с внешним ключом на `contracts`, `make db-migrate` проходит.
- [x] 1.4 В `ContractorService` добавить сохранение коллекции `property` по образцу `creditorsClaims`: создание, обновление и удаление записей по переданному списку. Проверка: функциональный тест — `PUT /api/v1/contracts/{id}` с двумя записями сохраняет их, повторный запрос без одной из них её удаляет.

## 2. Backend: расчёты и тексты для документов

- [x] 2.1 Создать `src/Service/Templates/Property/PropertyMethods.php`: строки описи по видам (при отсутствии имущества — строки подвидов формы с прочерками), строки и суммы таблицы 4.3 отчёта, итог «Всего имущества», перечни для анализа с «не выявлено». Суммы считать через `MoneyHelperService`. Проверка: юнит-тесты на дело с имуществом и без него, включая суммы с пробелами и запятой.
- [x] 2.2 Добавить в `Contracts` геттеры-обёртки для всех значений, которые используют шаблоны. Проверка: `make stan` зелёный, геттеры покрыты тестами из 2.1.

## 3. Frontend: блок в карточке дела

- [x] 3.1 В `src/components/ClientCard/types.ts` и `helpers.ts` описать запись имущества и нормализацию коллекции из ответа API. Проверка: `npx tsc --noEmit`.
- [x] 3.2 В `src/components/ClientCard/JudicialTab/Procedure.tsx` добавить секцию «Имущество» с подразделами «Недвижимое» и «Движимое»: добавление и удаление записей, выбор подвида, поля по образцу карточки требования кредитора. Проверка: `npx tsc --noEmit`, `npm run build`.
- [x] 3.3 В `src/components/ClientCard/index.tsx` передавать коллекцию имущества при сохранении. Проверка: запись, добавленная в интерфейсе, сохраняется и перечитывается после обновления страницы.

## 4. Документы

- [x] 4.1 В шаблоне отчёта финансового управляющего заменить строки «1. Недвижимое имущество всего» и «2. Движимое имущество всего» на суммы и FOR-блоки с перечнем объектов, а строку «Всего имущества» — на итог; красную пометку с образца снять. Проверка: тест шаблона — дело с квартирой даёт её строку и суммы, дело без имущества оставляет нули и прочерки.
- [x] 4.2 В шаблоне описи имущества заменить строки подвидов в таблицах «Недвижимое имущество» и «Движимое имущество» на FOR-блоки со строками из `PropertyMethods`. Проверка: тест шаблона — без имущества таблицы выглядят как форма, с имуществом появляются строки объектов.
- [x] 4.3 В шаблоне анализа финансового состояния подставить перечни в строки о недвижимом и движимом имуществе вместо текста «не выявлено»; красную пометку снять. Проверка: тест шаблона на оба случая.

## 5. Проверки и приёмка

- [x] 5.1 Прогнать в контейнере `bankruptcy-php` `make stan`, `make lint`, `make test`, во фронтенде — `npx tsc --noEmit` и `npm run build`; исправить все ошибки. Проверка: все команды завершаются без ошибок.
- [x] 5.2 Пройти сквозную проверку по `openspec/specs/documents/end-to-end-check`: добавить через интерфейс квартиру и автомобиль моковыми данными, сохранить дело без ошибок, скачать отчёт финансового управляющего, опись имущества и анализ финансового состояния и сверить строки имущества с введёнными данными. Проверка: в файлах нет `{{` и `${`, суммы и перечни совпадают с карточкой, ошибок в консоли и сети нет. Моковые данные не откатывать.
