## 1. Backend: виды, поля, хранение

- [x] 1.1 В `PropertyKind` добавить виды: денежные средства на счетах, наличные, акции и участие, ценные бумаги, ценное имущество, дебиторская задолженность, исключительные права; добавить признак наличия подписей подвидов. В `PropertySubtype` добавить подвиды ценного имущества (драгоценности, предметы искусства, имущество для профессиональных занятий, иное) и по одному подвиду на остальные виды. Проверка: юнит-тесты на состав подвидов каждого вида и на признак подписей.
- [x] 1.2 В `ContractsProperty` добавить поля: вид и валюта счёта, дата открытия счёта, сумма, валюта, лицо, выпустившее ценную бумагу, доля участия, количество, содержание обязательства, основание. Проверка: `make stan` зелёный.
- [x] 1.3 Сгенерировать миграцию Doctrine на новые колонки и применить её. Проверка: миграция добавляет только новые колонки, `make db-migrate` проходит.
- [x] 1.4 В `ContractorService::updateProperty()` сохранять новые поля, суммы — через `MoneyHelperService`, дату открытия — как дату. Проверка: функциональный тест — `PUT /api/v1/contracts/{id}` со счётом и дебиторской задолженностью сохраняет все поля.

## 2. Backend: строки, суммы и тексты

- [x] 2.1 В `PropertyMethods` перевести строки и суммы на список видов, добавить поля новых категорий в строку описи и параметр формулировки «не выявлено». Строки описи для видов без подписей подвидов начинать с наименования, а пустую таблицу выводить одной строкой прочерков. Проверка: юнит-тесты на каждую категорию — с записями и без.
- [x] 2.2 Добавить в `Contracts` геттеры для семи таблиц описи, пяти строк отчёта с суммами по группам и шести текстов анализа. Проверка: `make stan` зелёный, геттеры покрыты тестами из 2.1.

## 3. Frontend: подразделы категорий

- [x] 3.1 В `types.ts` и `index.tsx` добавить новые поля записи имущества. Проверка: `npx tsc --noEmit`.
- [x] 3.2 В `PropertySection.tsx` добавить семь подразделов с полями своей категории и подписями формы (остаток на счёте, сумма задолженности и так далее); подвид показывать только у ценного имущества. Проверка: `npx tsc --noEmit`, `npm run build`, запись каждой категории сохраняется и перечитывается.

## 4. Документы

- [x] 4.1 В шаблоне описи заменить строки семи оставшихся таблиц на FOR-блоки своих видов. Проверка: тест шаблона — без записей таблицы выглядят как форма, с записями появляются строки по колонкам.
- [x] 4.2 В шаблоне отчёта заполнить строки 3–7 таблицы 4.3 суммами по группам и FOR-блоками объектов. Проверка: тест шаблона на дело со счётом, дебиторской задолженностью и ценным имуществом.
- [x] 4.3 В шаблоне анализа подставить перечни в шесть строк вместо «не выявлены». Проверка: тест шаблона на оба случая, включая окончание «не выявлена» у дебиторской задолженности.

## 5. Проверки и приёмка

- [x] 5.1 Прогнать в контейнере `bankruptcy-php` `make stan`, `make lint`, `make test`, во фронтенде — `npx tsc --noEmit` и `npm run build`; исправить все ошибки. Проверка: все команды завершаются без ошибок.
- [x] 5.2 Пройти сквозную проверку по `openspec/specs/documents/end-to-end-check`: добавить через интерфейс записи новых категорий моковыми данными, сохранить дело, скачать отчёт, опись и анализ и сверить строки и суммы с введённым. Проверка: в файлах нет `{{` и `${`, значения совпадают с карточкой, ошибок в консоли и сети нет. Моковые данные не откатывать.
