# Environment files example
# Скопируйте этот файл в .env.local и настройте под себя

# Backend API URL (используйте относительный путь, чтобы dev-сервер Vite мог проксировать запросы)
VITE_API_URL=/api/v1

# Целевой адрес для прокси в dev-режиме
VITE_API_PROXY_TARGET=https://api.littlewell-app.work

# Другие переменные окружения можно добавить здесь
